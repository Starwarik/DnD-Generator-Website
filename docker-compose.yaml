services:

  web:
    build: ./backend
    ports:
      - 8004:8000
    volumes:
    #  - ./app/database.db:/code/app/database.db
      - ./data/database.db:/code/data/database.db 
    env_file: ".env"
    environment:
      - CELERY_BROKER_URL=redis://redis:6379/0
      - CELERY_RESULT_BACKEND=redis://redis:6379/0
    restart: unless-stopped  
    depends_on:
      - redis

#  worker:
#    build: ./app
#    command: celery -A worker.celery worker --loglevel=info --logfile=logs/celery.log
#    volumes:
#      - ./app:/usr/src/app
#    environment:
#      - CELERY_BROKER_URL=redis://redis:6379/0
#      - CELERY_RESULT_BACKEND=redis://redis:6379/0
#    restart: unless-stopped
#    depends_on:
#      - web
#      - redis

  redis:
    image: redis:7

volumes:
  data: